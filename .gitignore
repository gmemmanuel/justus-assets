# Agent skill mirrors synced from the justus app repo (scripts/sync-agent-skills.js).
# This repo is PUBLIC and served raw at justus.health (.nojekyll) — these must
# never be committed or they publish to the live site.
.agents/
.claude/
.factory/
.DS_Store

# Founder-only analytics dashboard: local IP config + generated output.
scripts/dashboard/config.local.json
scripts/dashboard/out/
scripts/dashboard/__pycache__/
